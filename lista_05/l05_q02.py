entrada = input().split(',')

def caca_tesouro(sequencia, posicao = 0):
    valores = sequencia.strip(',')
    if len(sequencia) > 0:
        #if sequencia[posicao]
        return

    return True

caca_tesouro("123445")